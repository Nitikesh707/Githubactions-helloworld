name: checkout-code

on: push
jobs:
  my-job:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Start application
        run: node server.js

      - name: List repository files
        run: Write-Output "Now closing MF'

        -name Sleepmf
        run: Start-Sleep 100

        -name stop
        run: Exit
